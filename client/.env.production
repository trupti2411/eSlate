# Read by Vite during `npx vite build`.
# Frontend (dev.eslate.com.au) calls the API (api.dev.eslate.com.au).
# Both must use the same protocol once SSL is provisioned to avoid mixed-content warnings.
VITE_API_BASE=https://api.dev.eslate.com.au
