# Read by Vite during `npx vite build` on Render.
# render.yaml ALSO sets VITE_API_BASE via its envVars block — that takes
# precedence in CI; this file is the fallback / local-build-of-prod sanity value.
VITE_API_BASE=https://eslate-api.onrender.com
